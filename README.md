# Message-Area

📝 Real-Time Character Counter Web Application

This project is a simple yet interactive character counter web application developed using HTML, CSS, and JavaScript. It allows users to type a message in a textarea while tracking the number of characters entered in real time.

🔹 Features

Displays the number of characters typed and the remaining characters allowed (maximum 200).

Updates the character count dynamically as the user types.

Prevents the user from entering more than the allowed character limit.

Shows a warning message when the character limit is reached.

Uses dynamic color changes (green, orange, red) to visually indicate character usage levels.

Includes a Clear Message button to reset the textarea and counter instantly.

🔹 Technologies Used

HTML5

CSS3

JavaScript (DOM Manipulation & Event Handling)

🔹 Purpose

This project demonstrates real-time input handling, conditional logic, and DOM manipulation in JavaScript. It enhances user experience through visual feedback and input constraints, making it suitable for beginners, academic lab submissions, and front-end practice projects.
